# Secret-scan allowlist (docs/secret-scan.md). One extended-regex per line; a
# candidate added line matching any regex is NOT flagged by the builtin scanner.
# Keep entries PRECISE — a broad regex defeats the gate. Comments / blank lines
# are ignored. (gitleaks, when present, uses its own .gitleaks.toml instead.)
#
# Every entry here is a TEST FIXTURE in test/secret-scan.bats — synthetic, never
# a live credential. They are committed so the gate can be exercised against real
# secret shapes without tripping on its own test suite.

# AWS's official non-functional documentation example key.
AKIAIOSFODNN7EXAMPLE

# Deterministic fake GitHub-token fixture (sequential digits — obviously synthetic).
ghp_0123456789012345678901234567890123ab

# The fake OpenSSH private-key fixture line: anchored to its unique synthetic
# base64 body so this allows ONLY that test line, never a real PRIVATE KEY block.
b3BlbnNzaA==
