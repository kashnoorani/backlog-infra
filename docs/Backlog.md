# backlog-infra — Backlog

A running list of work to do. Mark items `- [x]` when done and move
them to the Done section, `- [ ]` when open, `- [~]` when in progress,
`- [?]` when blocked on a design decision the user needs to weigh in
on, and `- [!]` once you've written answers inline and want the worker
to retry. The autonomous worker (`backlog run`) picks `- [ ]` and `- [!]` items and skips `- [?]`; surface those
to the user when waking them.

Items parked in `## Thinking` are never picked by the daemon — move
them to `## Open` when ready for work.

## Thinking

> **Rationalization (2026-05-28) — read before promoting items to `## Open`.**
> Each item is tagged **(priority · wave)**.
> **Priority:** P1 = prevents unattended damage / makes the fleet observable / foundational unblocker · P2 = output quality + correctness · P3 = larger architectural or meta-maintenance.
> **Wave (build order, derived from dependencies):** W0 foundations → W1 primitives → W2 consumers → W3 orchestration → W4 lifecycle.
>
> **Shared primitives — build once, then the tagged items are thin consumers (don't implement 5 bespoke versions):**
> 1. **Pause mechanism** (scope `item | project | fleet` → heartbeat-only, with reason + until-condition): account-cooldown, per-project budget, per-item ceiling, fleet-freeze, pause/resume.
> 2. **Tick guard + abort/reopen/annotate** (a check that can veto a tick): per-tick verification, circuit breaker, diff-size, secret-scan, protected-path, post-completion review.
> 3. **One notify/event channel** (extend `agent-notify.json`): dead-man's-switch, fleet digest, brittle-contract canary (+ existing cooldown alert).
> 4. **Structured event log** → feeds metrics, alerting, digest, monitor.
> 5. **D1 telemetry bus + liveness** (COMMITTED) → underpins the account-cooldown signal, fleet-freeze flag, version-skew, live dashboard. Sequence early as an enabler.
> 6. **Test/CI harness** → gates safely changing the driver for everything else. Do first.
> 7. **Backlog integrity**: schema lint + snapshot + the completion-flag recording item (in `## Open`).
>
> **Resolved decisions (2026-05-28):** D1 telemetry bus = **committed**. Compaction = **Archive (shared `backlog-agent-compact.mjs`) is canonical** — align per-project scripts. Branch-per-item isolation = **rejected**, in favor of commit-to-main + the damage guards (diff-size, verification, protected-path).

- [ ] **(P1 · W0) Backlog schema validation / linter.** The queue is hand- and agent-edited markdown, and that causes breakage — e.g. "web dashboard item back to `[?]` (agent flipped it)" and the `auto_flip_blocked` `[user]`-at-line-start bug. Add a validator (git pre-commit hook or top-of-tick guard) asserting: sections in canonical order, every item a valid marker, no item in two sections, no duplicate titles, `[~]` items have an inflight owner. Makes the markdown queue robust to malformed edits instead of trusting every writer.
- [ ] **(P1 · W0) Backlog snapshot safety net.** The backlog is the single source of truth, mutated by autonomous agents — and sacred-geography's `docs/Backlog.md` dropped 2,033 lines in one pull. Git is the history, but an explicit periodic snapshot (daily tag, or off-repo copy) makes "an agent nuked a backlog" a one-command restore instead of git archaeology. Cheap insurance.- [ ] **(P1 · W1) D1 telemetry bus + decouple liveness from commits (COMMITTED — enabler, sequence early).** Riding ephemeral status (heartbeat, tokens, liveness) on commits to shared `.claude/` files is the root cause of a whole incident class: phantom tokens, orphaned-stash churn, per-host file conflicts, the untrack sweep. Finish the **Path B (D1-backed realtime health ingest)** migration started in backlog-dashboard: git keeps only durable, mergeable state (`docs/Backlog.md`, work commits, the per-tick ledger); telemetry pushes to D1. **Critically:** `reclaim_stale_claims`'s liveness signal is "a status commit in the last 90m" — exactly what broke today, so a failed status hook makes the reaper unable to tell a live-but-not-committing daemon from a dead one (correctness hazard). Moving liveness onto the independent D1 heartbeat fixes the reaper too. **Free-tier constraint (must stay on CF free):** D1 free limits are 100k rows written/day, 5M rows *read*/day, 5 GB storage (verified 2026-05-28, [pricing](https://developers.cloudflare.com/d1/platform/pricing/)). At ~1,500 ticks/day fleet-wide, writes (~4.5k/day), requests, and storage are all <5% of the ceiling — the only blow-up risk is **read-row amplification**, since D1 bills rows *scanned*, not returned: an unindexed dashboard poll over a growing history table on a short refresh loop can hit 100s of millions of reads/day. Design to avoid it: (a) a tiny **current-state table** (~1 row per project×host) for the live dashboard view, with the append-only history reserved for analytics; (b) **index history** on `ts`/`project`/`host`; (c) **push (SSE/WebSocket) over polling** from the ingest Worker; (d) a **retention/rollup** job on history. → enables: account-cooldown signal, fleet-freeze flag, version-skew, live dashboard. **[2026-05-28 draft ready: `docs/d1-telemetry-schema.md`]** — investigation found Path B ~50% built in backlog-dashboard (ingest + read endpoints + push already live; schema is account-only with no DDL in repo, no history table, dashboard polls every 15s, liveness still git-derived). DDL for `health_status` (current-state) + indexed `health_history` is written, ready to lift into a migration. **Decisions RESOLVED 2026-05-28: DECISION 1 = reaper fail-safe (no reclaim when D1 unreachable) · DECISION 2 = poll the indexed table + defer SSE.** Remaining sequence: (1) land migration in backlog-dashboard, (2) ingest/status-hook write `heartbeat_epoch` (both additive, safe now); (3) switch `reclaim_stale_claims` to the D1 heartbeat — **gated on version-skew + canary first; daemon stays DOWN until then.** **[2026-05-28] steps (1)+(2) DONE + LIVE** — `migrations/0001_telemetry.sql` applied to remote D1 (`heartbeat_epoch`+`driver_sha` columns + indexed `health_history` verified); defensive ingest deployed to Pages; shared hook sends `heartbeat_epoch`; end-to-end POST round-trip confirmed. **[2026-05-28] step (3) reaper switch IMPLEMENTED (ships dark)** — `reclaim_stale_claims` in `bin/backlog-agent` now reads liveness from the D1 `heartbeat_epoch` (new `_d1_heartbeat_epoch`/`_health_project`/`_health_host`/`_urlencode` helpers) instead of git-commit recency; the git-log probe + `last_status_ts` are removed (DECISION 1 fail-safe: empty/unreachable/404/null → no reclaim). `test/tick.bats` +6 cases (live/dead/unreachable/notfound/startup/keyless), `make_curl` shim + HOME-redirect in `helpers.bash`; full suite green (78). Fixed a spec bug: naive `"${auth[@]}"` is an unbound-variable error under bash 3.2 + `set -u` on keyless machines — switched to `${auth[@]+"${auth[@]}"}` (regression-tested). **Gates before going live:** (a) **prereq — DONE 2026-05-30** — `GET /api/heartbeat` now SHIPPED + deployed in backlog-dashboard (`functions/api/heartbeat.js`, live at `kash-backlogs.pages.dev/api/heartbeat`; verified `?project=…&host=…` returns `{found,heartbeat_epoch,last_tick_at}`, missing-project → 400, no-row → `{found:false}`). The reaper's D1 reads now resolve, so the switch is no longer inert on this prereq — only gate (b) remains; (b) **canary (W3) + version-skew** before daemons come up. `health_history` writes deferred to the W2 metrics consumer.
- [ ] **(P2 · W1) Driver version-skew visibility + on-demand fleet sync.** M1 ran the broken driver for hours purely because the fix was on origin but unpulled, and `backlog-agents daemon-sync` is on a 24h cycle — and because `bin/` is *shared*, a fix isn't live until every machine pulls. Make "is everyone running the fix?" a glance: (a) stamp each tick's status with the **git SHA of `backlog-infra/bin`** the daemon is running; the dashboard surfaces skew (`M1 abc123 · M3 def456 · origin def456 ⚠`); (b) add an on-demand fleet sync trigger so deploying a driver fix doesn't wait up to 24h. → feeds: fleet-freeze (skew-zero is its clear condition). **[2026-05-28] (a) SHIPPED** — driver stamps `driver_sha` (last commit touching `bin/`) into status + per-host + history via `--driver-sha`; `backlog-agents` MACHINES table shows a DRIVER column flagging drift vs `origin/main` bin (red `⚠`) + a `driver ref` line. Tested (status-hook stamping) + verified live (matched green / skewed `deadbee ⚠`). **(b) DEFERRED** — cross-machine on-demand trigger rides the D1 freeze-flag (daemons are down; no cross-machine channel until D1); local on-demand already = `backlog-agents sync`.
- [x] **(P1 · W1) Prompt-injection / malicious-backlog threat model.** Daemons run `claude -p` with broad shell/commit permissions and the *backlog item text is the prompt* — a poisoned item (via a compromised repo, a dependency README, or a synced file) could instruct an agent to exfiltrate secrets or push malicious code, unattended. Everything else is output-side (secret-scanning, diff guards); nothing vets the *input*. Define a trust boundary on backlog additions (who/what may append `[ ]` items) and a constrained tool policy for autonomous mode. Highest-value security gap. **[2026-05-28 draft ready: `docs/threat-model.md`]** — trust model (5 entry paths, key risk E4/E5→E2 indirect injection), trust boundary on additions, and a deny-by-default autonomous tool policy are defined. Two **[DECISION]** points gate enforcement: force agent-appended items into `## Thinking` (human-promote)? · deny-by-default network/secret-path tool profile for `claude -p`? Enforcement pieces map onto the W2 protected-path / secret-scan / diff-size items. **[2026-05-30 INPUT-SIDE ENFORCEMENT SHIPPED — driver bin `725afff`.]** Both DECISIONs resolved (user): (1) force agent-appended `## Open` items into `## Thinking` = YES; (2) deny-by-default tool profile = YES but split to a follow-up (default-off first). Landed the first slice (driver-only, hermetic): **(a) §4.3 untrusted-content firewall** — the tick prompt now tells the agent that content read from files/URLs/command-output while working is DATA not instructions (no new backlog items, no out-of-scope commits, no secret access, no off-machine flow; follow-ups → `## Thinking`); **(b) Guard 7** (`_relocate_injected_open_items`, post-tick) — any executable `[ ]`/`[!]` item that appeared in `## Open` THIS tick (other than the claimed item) is relocated to `## Thinking` (daemon-untouchable) + flagged (`log_event injection_item_relocated` + `notify_send` + scoped commit). FAIL-OPEN; gated by `INJECTION_GUARD_DISABLE`. Closes the E4/E5→E2 indirect-injection vector. `test/injection-guard.bats` (8); suite **232/232**; canary GREEN for `725afff`. `docs/threat-model.md` updated (status → input-side enforcement shipped). **Remaining follow-up: the §5 deny-by-default `claude -p` tool profile** (CLI flags verified on v2.1.158: `--disallowedTools`/`--permission-mode`/`--settings`/`--add-dir`) — ship behind a default-off flag. **[2026-05-30 §5 SHIPPED behind default-off flag — `docs/tool-profile.md`.]** Driver-only, hermetic. New opt-in flag `AUTONOMOUS_TOOL_PROFILE` (default 0; the inverse of the default-enabled `*_DISABLE` flags, since a too-broad deny silently breaks ticks). When 1, the `claude -p` autonomous tick gets a generated inline `--settings` JSON (`build_tool_profile_settings`, jq-free) carrying **only** `permissions.deny` (deny wins + layers onto the daemon's config, so the headless auto-accept is untouched — no `defaultMode`/`allow`): network egress (`WebFetch`/`WebSearch` + `Bash(curl|wget|nc|ncat|telnet:*)`), secret-path reads (`~/.ssh`/`~/.aws`/`~/.gnupg`/`~/.config/backlog`/`.env`), and Edit+Write of each `PROTECTED_PATHS` file (the preventive twin of the detective Guard 3; exact names, not blanket `bin/**`, so a project's own tooling stays writable). `log_event tool_profile_applied` telemetry; the Layer-3 fallback agent is left unprofiled. `test/tool-profile.bats` (9); suite **242/242**; canary GREEN for the new driver SHA. **Landed default-off only — flip-on per-project (then fleet-wide), with a live spot-check of the path-rule matching semantics, is a SEPARATE greenlit step.**
- [ ] **(P2 · W2) Structured "done-when" acceptance criteria per item.** Items are freeform prose, which gives the verifier nothing concrete to assert and is why "agent thinks it's done but isn't" happens. An optional `done when:` field gives per-tick verification a real target and tightens the definition of done.
- [ ] **(P1 · W2) Diff-size guardrail on work commits.** [guard-primitive consumer] Flag/pause any tick whose `work_commit` touches more than N files or M lines unless the item opts in — exactly the guard that would have caught sacred-geography's 2,033-line backlog deletion. Change-*size* guard; complements the failure-*count* circuit breaker. **[2026-05-31 reconcile — PARTIAL: the flag shipped (Guard 2, `bin/backlog-agent:3046`); the pause + opt-in did NOT.]** Guard 2 emits a `[WARNING]` + `log_event diff_oversize` when a tick exceeds `DIFF_SIZE_MAX_FILES=20` / `DIFF_SIZE_MAX_LINES=400`, but it is **flag-only** — it does NOT pause the tick, and there is no per-item opt-in to suppress it. Remaining gap = (a) actually **pause/heartbeat-hold** an oversize tick (rides the shared pause primitive), (b) a per-item opt-in so a legitimately-large item isn't flagged. NB: flag-only is currently the deliberate stance across Guards 2/3/5/8 ("observe signal quality before vetoing") — confirm pausing is still wanted before building it.
- [ ] **(P2 · W2) Protected-path list.** [guard-primitive consumer] Files like the driver `bin/backlog-agent`, `release.mjs`, `CLAUDE.md`, `.gitignore` should never be modified by an autonomous tick without flagging for review — stops an agent from silently editing its own controller. → also the path-based signal fleet-freeze uses to classify a change as fleet-affecting. **[2026-05-31 reconcile — MOSTLY SHIPPED; only the secondary "→" clause is open, and it's moot.]** The primary ask shipped: **Guard 3** (detective) flags any committed protected file (`PROTECTED_PATHS` in `bin/_lib.sh:45` = `bin/backlog-agent bin/backlog-agent-status.mjs release.mjs CLAUDE.md .gitignore`; `log_event protected_path_touched` at `bin/backlog-agent:3067`), and the **§5 tool-profile** deny is its preventive twin (Edit/Write of those paths denied to `claude -p` when the marker is set). Remaining = the "→ fleet-freeze classifies via `PROTECTED_PATHS`" clause, which was **never wired and is now moot**: fleet-freeze uses **canary-pass + skew-zero** as its clear-condition (`backlog-agents` freeze `--eval`), not a path-based fleet-affecting classifier. Decide whether to retire the "→" clause or repurpose `PROTECTED_PATHS` as a freeze input; the protected-path guard itself is done.
- [ ] **(P2 · W2) Efficiency metrics from the per-tick ledger.** The JSONL ledger is only surfaced as burn rate today. Roll it into decision data: cost per completed item, success rate per project, tokens-per-completion trend, time-to-completion, retry rate. Answers "which projects are expensive per item?" and "which items keep failing?". → depends on: the completion-flag recording item in `## Open`, structured logs. **[2026-05-31 reconcile — MOSTLY SHIPPED: the core rollup landed as `backlog-agents metrics`.]** `do_metrics()` (`bin/backlog-agents:2757`) reads each project's per-tick ledger and prints a per-project table + fleet summary of total ticks, completed items, **tokens/completion**, **success rate**, and **retry rate** (`--json` for machine output). Remaining gaps: (a) **time-to-completion** (no wall-clock duration metric yet); (b) **a real completion flag** — "completed" is currently APPROXIMATED as `exit_code==0 && non-empty work_commit`, which over/under-counts (footnoted in the output); a true completion field in the ledger would make every rate exact; (c) **trend / time-series** views (tokens-per-completion over time) — today the metrics are point-in-time aggregates, not trends. "Cost per item" is approximated by tokens/completion (token-based, not dollar-cost).
- [ ] **(P2 · W2) Guard the brittle external contracts.** [notify-channel consumer] Cooldown detection is **regex-matching Anthropic's error text** — if they reword "usage limit reached," cooldown silently stops working and the fleet returns to fail-loops. Same fragility with `claude` CLI flags and `jq`/`gawk`/`fswatch`/`wrangler` versions. A scheduled canary test asserting "the plan-limit signature still matches a known sample" and "claude still accepts our flags" catches upstream changes before they break the fleet silently.
- [ ] **(P2 · W3) Canary rollout for driver changes.** Because `bin/` is shared, one bad driver commit breaks all 7 projects at once — which is what the cooldown-add bug did. Before a driver change goes fleet-wide, run it against one **canary project** for a cycle and assert health, then let the rest pull. Blast-radius control. → the "canary passes" half of the fleet-freeze clear-condition. **[2026-05-28] gate mechanism SHIPPED** — `backlog-agents canary` validates the current driver (hermetic self-test suite — which holds the cooldown-bug regression test — + `doctor` hard-fail check) and records a pass keyed to the driver bin SHA in `~/.claude/backlog-canary.json`; `canary --check` is the read-only gate (exit 0 iff the running driver passed) the reaper-restart + fleet-freeze consult. `test/canary.bats` (6 tests). **Residual (folds into fleet-freeze):** the live one-project-tick variant + auto-gating `daemon-sync` propagation on `canary --check`.
- [ ] **(P2 · W3) Priority-weighted fleet scheduling.** All 7 daemons tick independently and compete first-come for the shared account budget. Add a project priority so high-value work gets more frequent ticks / wins contention; the elegant version is a single fleet scheduler allocating the shared token budget by priority rather than 7 independent loops. → underpins the infra=highest-priority half of fleet-freeze.
- [ ] **(P2 · W4) New-machine bootstrap drill + incident runbook.** If a machine's disk dies, how fast can a replacement be stood up? The manifest + `kash_setup.sh` exist, but an actually-*tested* "bare machine → fully-running fleet" drill — plus a written incident runbook (symptom → check → fix, codifying what was diagnosed by hand during the cooldown incident) — turns recovery from archaeology into a checklist. `doctor` is the automated half; this is the human half.
- [ ] **(P3 · W4) Dead-project / archive-candidate detector.** Some projects' backlogs may be effectively dead — no open items for weeks, just burning heartbeat cycles and dashboard space. Auto-flag projects that should move to `archived/` (ties to `dev-projects archive`), keeping the active fleet lean.
- [ ] **(P3 · W4) Weekly backlog-hygiene self-audit tick.** A scheduled agent that sweeps each backlog for stale items, duplicates, things parked in Thinking forever, and mis-marked states, then proposes cleanups — meta-maintenance so the queue doesn't rot.
- [ ] **(P3 · W4) Align compaction on the Archive strategy (decided).** Canonical = the shared `bin/backlog-agent-compact.mjs` (Done → Archive); `architecture.md` §6/§10 already document this. Replace per-project *title-only* variants (e.g. sacred-geography's `scripts/compact-backlog.mjs`) with the shared behavior so every project compacts identically. → unblocks the trigger fix below.
- [ ] **(P3 · W4) Fix the auto-compact trigger.** `_post_tick_cleanup` gates the compact call on `done_count > 20`. Under the Archive strategy this clears correctly (compacted items leave `## Done`), so the fix is: remove the title-only special-casing and confirm the count actually drops post-compact once all projects use Archive. Depends on the alignment item above.
- [ ] **(P3 · parked — scale only) Per-item claims to kill the single-file CAS bottleneck.** Every claim/unclaim/reclaim is a full-file CAS edit on one `docs/Backlog.md`, and the awk-inplace footgun lives there. Fine at 2 machines / 7 projects; if the fleet grows, an append-only claim log or one-file-per-item would cut contention and eliminate the file-rewrite class. Not now.

- [ ] **(P2 · W0) Track `.claude/backlog-agent-events.jsonl` so the dashboard Guard tab lights up.** The dashboard Guard/event-stream view is BUILT (backlog-dashboard `fleet.js` fetches `_events` via GraphQL + `renderGuard`) but DARK because the events stream is `.gitignore`d in every repo — the dashboard reads committed files from GitHub, so an un-committed events file is invisible. Reverse it the SAFE way (mirror the already-tracked, append-only, union-merged `backlog-history.jsonl` — NOT a naive un-ignore, which would re-introduce the orphan-stash leak that motivated gitignoring it): (a) in the shared status hook `bin/backlog-agent-status.mjs`, add the events log to the explicit staged set (~line 785) AND extend `EPHEMERAL_RE` (~line 116) to include `backlog-agent-events\.jsonl` so concurrent cross-host appends union-merge on a rebase pop-conflict (the line-176 `merged` branch); (b) remove the `.claude/backlog-agent-events.jsonl` line from every repo's `.gitignore` + the `templates/dev-project/.gitignore`; (c) MOVE events OUT of the `EPHEMERAL_CLAUDE_IGNORES` SSOT in `bin/backlog-agents` (and its `test/doctor.bats` parity test) — else `doctor` HARD-FAILS on the now-tracked file fleet-wide. Publish-safety already cleared: `secret_detected` events store only the item title (public) + a `<sha>..<sha>` range, never the secret value. Fleet-wide hook behavior change → `backlog-agents canary` green, then it lands on daemons via a gated `backlog-agents sync --restart-only` (no restart strictly needed — the hook is re-exec'd each tick — but follow the discipline). Verify: `git check-ignore` returns nothing, a tick commits the events file, the live Guard tab populates, no orphan stash after a cross-machine rebase. [the deferred 'INFRA HALF' of the dashboard Guard work, confirmed still pending 2026-05-31]

- [ ] **(P2 · W3) Research: stop monitor rebase from nuking follower working trees.** The M1 monitor rebases origin on every sweep, rewriting commit SHAs. This causes `_follower_reconcile` to detect divergence and `reset --hard` on M3 for EVERY project on every `backlog-agents sync --projects-only` — jarring and productivity-killing. The tripwires (clean tree + only `monitor:` commits ahead) prevent data loss, but the constant resets still disrupt interactive work. Research options: (a) **flip reconcile to opt-in** — `sync` warns on divergence but doesn't auto-reset; `--reconcile` flag for the current behavior; (b) **make the monitor not rebase** (use merges instead) — eliminates the root cause entirely but changes commit history shape and affects the test suite; (c) **exclude interactive/protected repos from reconcile** (e.g. skip `backlog-infra` + optionally any repo with a dirty tree). Evaluate each option's impact on: cross-machine sync correctness, monitor sweep throughput, CI/test compatibility, and operator UX. Implement the chosen approach.

## Open

## Done
- [x] **(P2 · W2) Post-completion review pass.** SHIPPED 2026-05-30 — driver bin `34a21cf`; `docs/post-completion-review.md`. [guard-primitive consumer] **Guard 8** in `tick_once`: after a tick COMPLETES an item (`[x]`) and survives the gate + ceiling, `post_completion_review()` diffs `work_base..work_head` against the item title+body with a cheap `claude -p --model claude-haiku-4-5` agent and flags intent-vs-change mismatches incl. unrelated collateral edits — the failure a green build gate (Guard 4) is blind to (the TaizMail-canary class: "edited the backlog" but also dropped an unrelated `[?]` item; nothing breaks). **ANNOTATE-ONLY** (DECISION 1, user): `log_event post_completion_mismatch` + `notify_send`, the `[x]` STANDS — no reopen (sibling of the flag-only Guards 2/3/5; observe signal quality before vetoing). **OPT-IN, canary-first** (DECISION 2): inert unless `POST_COMPLETION_REVIEW=1`, flipped per-project via the per-machine marker `~/.claude/post-review/<project>.on` (new `post_review_plist_env` in `_lib.sh` threads it into the daemon plist — same mechanism as §5 tool-profile). **Cost ceiling** (DECISION 3): `POST_REVIEW_MAX_LINES=2000` — over it the review is SKIPPED + logged (`post_completion_review_skipped reason=oversize`), no silent caps. FAIL-OPEN (missing claude / empty diff / unparseable verdict ⇒ accept). Uses the 2-arg `notify_send` so it's independent of the parallel Telegram-HTML stream it rebased onto (`8765587`). `test/post-completion-review.bats` (8); suite **261→269**; shellcheck + secret-scan clean; canary GREEN for `34a21cf`. Driver + CLI-test + docs only — NO D1 / dashboard change; ships inert until a marker is set.
- [x] **(P3 · W2) Monitor: stop re-flagging a HELD daemon's frozen log every sweep.** [promoted from Thinking 2026-05-30 — generates the cross-machine monitor-sweep divergence] Surfaced 2026-05-29 while installing the monitor for the dead-man's-switch: the held infra daemon's `.claude/backlog-agent.log` is frozen with stale `failed to push some refs` lines, so the monitor's Patterns 1-3 match EVERY 120s sweep. The destructive half is fixed (it now FLAGS the infra repo, never stash/resets it — commit `03e9d74`), but it still (a) prints a FLAG + increments `flagged_total` every sweep, and (b) `_write_status` commits+pushes `monitor-status-<host>.json` to backlog-infra on every flagged sweep → ~720 status commits/day of churn. Fix options: detect a daemon is HELD (not loaded in launchctl) and skip its log patterns entirely; or de-dup a repeated identical flag (only flag on a *transition*); or only commit monitor-status on a real fix/heartbeat, not a repeated infra flag. Cosmetic, not dangerous. **[2026-05-30 SHIPPED — driver bin will be the new origin SHA after this commit.]** Chose the **de-dup** option (`bin/backlog-agents` `do_monitor`): new `_infra_flag_seen`/`_infra_flag_rearm` helpers (mirroring the `_dms_alerted` per-incident dedup, `|`-delimited, bash-3.2-safe, monitor-process-scoped) gate the infra-repo flag in Patterns 1-3 (git_dirty / git_ff_conflict / git_push_rejected) so a FROZEN held-daemon log flags ONCE per incident instead of every 120s sweep. Because a deduped repeat is no longer an "action", `_write_status` stops committing every sweep — only the 300s heartbeat commit remains, killing the ~720 status-commits/day of churn that drove the cross-machine monitor-sweep divergence. Each pattern's `else` branch re-arms when the condition clears, so a genuinely new occurrence flags again. `test/dead-mans-switch.bats` +1 (a7: flag emitted once across multiple `--interval 1` sweeps + steady-state "OK (no issues)"); suite **233/233**; canary GREEN.
- [x] **(P2 · W2) Follower-update command must reconcile, not `pull --ff-only`.** Surfaced 2026-05-29 during the circuit-breaker fleet rollout: the documented follower-update / M3-rollout command `git pull --ff-only origin main` **always fails on a follower** because the M1 monitor rebases origin as it sweeps (rewriting commit SHAs), so a follower that earlier ff-pulled that history now diverges (`fatal: Not possible to fast-forward`). The daemon self-heals this in-tick (test 195, "diverged local HEAD is reset to origin"), but the human-facing rollout command doesn't — it strands the operator with a confusing divergence (~11 stale monitor-sweep commits that already exist on origin under new SHAs). Fix: make the documented follower-update sequence `git fetch origin && git reset --hard origin/main` (matching the daemon's own reconcile), or fold the reconcile into `backlog-agents sync` so the operator never runs a raw git pull. Update WORKFLOW.md / any rollout runbook accordingly. Low-risk, cosmetic-but-confusing. **[SHIPPED 2026-05-30 — fleet-CLI bin `b45fe65`.]** `backlog-agents sync` now RECONCILES monitor-churn divergence instead of the bare `pull --ff-only` that always failed on a follower. New `_follower_reconcile` (`bin/backlog-agents`): ff fast-path, else `fetch + reset --hard origin/<branch>` gated by TWO tripwires so real work is never nuked (clean tree AND every commit-ahead matches `^monitor:`); `SYNC_RECONCILE_DISABLE=1` opts out; new side-effect-free `sync --projects-only` seam. Dotfiles pull left ff-only (no monitor → divergence = real edits). Runbook `incident-runbook.md` B3 + quick-ref → `reset --hard origin/main` (clean tree) w/ a why-note. `test/sync-reconcile.bats` (6). Fleet-CLI / daemon-sync tool ONLY (not the project-daemon driver) → NO fleet restart; effective next `backlog-agents sync`. suite **252/252**.
- [x] **(P1 · W0) Test harness + CI for the driver itself.** [promoted from Thinking 2026-05-30 — W0 keystone, do before any other driver item] `bin/backlog-agent` (~1300 lines bash) and `bin/backlog-agent-status.mjs` run the whole fleet with zero automated tests — every incident so far (unconditional `git add COOLDOWN_FILE`, BSD-awk `-i inplace` silent no-op, the `reclaim_stale_claims` runaway, the `pipefail` crash) is the class a test suite catches. Add: (a) a **bats** harness that spins up two throwaway clones of a scratch repo as fake "M1"/"M3" and asserts the §5 invariants — claim CAS picks one winner + loser `reset --hard`s, TTL reaper frees `[~]` after 90m, cooldown short-circuits to heartbeat, reclaim-before-claim keeps a non-zero diff; (b) **shellcheck** on `bin/*` in CI (would have flagged the pipefail/quoting bugs); (c) a `--inject-fault` mode (kill mid-claim, simulate rejected push) to actually exercise the §10 recovery paths instead of trusting them. → unblocks safe work on every other driver item. **[SHIPPED 2026-05-30 — driver bin `6a8fe89`.]** Audit found the "zero tests" premise STALE (a 243-test bats suite + CI already existed); the net-new was genuine cross-machine coverage + a blocking lint. **(a)** `test/integration-twoclone.bats` (3) — a real bare-origin + two driver clones (M1/M3) running `backlog-agent tick` for real, asserting the §5 invariants CROSS-CLONE: two clones claim different items (no collision), a completed item is never double-worked, a dead clone's `[~]` is reaped + handed off with reclaim-before-claim kept a non-zero diff (fixed a fixture gap: the shared `complete` stub only commits, but a cross-clone test needs the completion PUSHED). **(b)** shellcheck flipped to BLOCKING + extended from `backlog-agent`+`_lib.sh` to all six `bin/*` scripts; cleared every `-S warning` (dead vars, `@{u}` quote, `# shellcheck shell=bash` on `_lib.sh`, scoped disables w/ reasons); CI `continue-on-error` removed; `test/run.sh` lints all six + fails on any new warning. **(c) `--inject-fault` DEFERRED** to a follow-up (the harness is its substrate; the true concurrent CAS push-rejection race needs mid-tick instrumentation). Test/CI-only — NO daemon behaviour change, no fleet restart. suite **246/246**; canary green.
- [x] **(P1 · W0) Ephemeral `.claude/` gitignore enforcement (doctor + template).** SHIPPED 2026-05-30. Audit found this ~90% already on HEAD — the SSOT array `EPHEMERAL_CLAUDE_IGNORES` (`bin/backlog-agents`), `doctor` enforcement (tracked→hard-fail / unignored→warn), `test/doctor.bats` coverage, and a superset `templates/dev-project/.gitignore` all existed; the rescue-tag `_lib.sh` `ephemeral_claude_files()` helper (tag `orphan-rescue-m3-ephemeral-gitignore`, `13fc6c0`) was the older/superseded form, not lifted. **The one real gap closed:** the SSOT array was MISSING two per-host ephemeral files the driver actually writes — `.claude/backlog-agent-hookfail.json` (dead-man's-switch) and `.claude/backlog-agent-budget.json` (per-project budget) — added to the template `.gitignore` in later slices but never back-filled into the doctor SSOT, so `doctor` didn't guard them (the exact orphan-stash leak class this item targets). Fix: added both to `EPHEMERAL_CLAUDE_IGNORES` + a hermetic `test/doctor.bats` **parity test** asserting the SSOT array and the template's `.claude/` patterns stay in exact lockstep (the missing invariant that let the drift through), + synced the `mk_project` fixture `.gitignore`. Kept the SSOT in `bin/backlog-agents` (NOT moved to `_lib.sh` — its only consumer is `doctor`; minimal blast radius). Driver/CLI + template + test only — NO D1/dashboard change. `test/doctor.bats` 9 cases; suite **243/243**; canary green for the new driver bin SHA.
- [x] **(P1 · W2) Automated fleet digest (digest half).** SHIPPED 2026-05-29 (`docs/fleet-digest.md`). [notify-channel consumer] A scheduled summary of *what each project accomplished this period* — completions, ticks, token spend, and the open `[?]` blockers that need a human — pushed via the shared notify channel. New `backlog-agents digest [--period day|week] [--json] [--no-notify]`: reads each project's LOCAL per-tick ledger `.claude/backlog-history.jsonl` (same source as `metrics`), windows rows by `ts` (last 24h / 7d), and reports per-project completions (exit_code==0 + non-empty `work_commit`, the `metrics` approximation), ticks, token spend, and the count + verbatim titles of open `[?]` blockers (scanned from `docs/Backlog.md`, the actionable unblock targets) + a fleet roll-up; `--json` emits a machine-readable object, `--no-notify` previews to stdout. `backlog-agents install-digest [--uninstall]` installs two launchd `StartCalendarInterval` timers on this machine (DAILY ~09:00 + WEEKLY Mondays ~09:00) — local because the data is local (a CF worker can't see the ledgers without D1 plumbing). **Notify refactor:** `notify_send` moved `bin/backlog-agent`→`bin/_lib.sh` (ONE shared sender for the driver's alerts AND the digest) + a new **Telegram** transport (`curl --data-urlencode` to the Bot API, jq-free) alongside Slack/email/macOS-local. **DECISIONS (user, 2026-05-29):** digest first (remote-unblock split to its own item); BOTH daily+weekly; local launchd run-home; delivery = macOS-local + Telegram (WhatsApp declined — bigger Meta/Twilio lift). **Prerequisite (user, outside code):** Telegram is INERT until a `@BotFather` bot token + chat_id are added to `~/.claude/agent-notify.json` (today empty slack/email + `local_notify:true` ⇒ macOS-local banner only; fail-open). Driver+CLI only — **NO D1 migration / dashboard change.** `test/digest.bats` (7); suite **219/219**.
- [x] **(P1 · W2) Item-level circuit breaker / retry budget.** SHIPPED 2026-05-29 (`docs/circuit-breaker-retry-budget.md`). [guard-primitive consumer] The *completion* of Guard 1 (the consecutive breaker shipped earlier as `1cefe2b`). Motivating incident: "Daemon broken" was claimed/unclaimed 6× in one window — a silent token-burning retry storm. Three missing pieces, now added: **(a)** the **failure reasons** are attached to the `[?]` note (the failcount state grew from a bare int to a record `{count, attempts, reasons:[]}`, back-compat with the old int form; the three bump sites — exit≠0 `claude exit N`, Guard-4 `verify: exit N`, Guard-6 `ceiling: <breach>` — each pass a short reason, last K=3 rendered into the note); **(b)** a trip now **surfaces via `notify_send`** (one push per trip, mirroring the dead-man's-switch/budget alert-once pattern; macOS-local-only until a webhook/email is set); **(c)** the cumulative **"retry budget"** — a SECOND counter (`attempts`) reset ONLY on completion (`[x]`), so a FLAPPING item (fail→partial→fail→…) that never strings K consecutive failures together still trips at `RETRY_BUDGET` total attempts (the consecutive counter resets on any clean tick). **DECISIONS (user, 2026-05-29):** add the retry budget now, `RETRY_BUDGET=6` (matches the 6-retry incident), reset on `[x]` only; enrich the existing failcount file (back-compat); env globals (`CIRCUIT_BREAKER_THRESHOLD`/`RETRY_BUDGET`) + an optional per-item override in a committable `.claude/backlog-guards.json` (`{circuit_breaker_threshold, retry_budget, items:{<substr>:{…}}}`, precedence env→top-level→item, mirroring `backlog-ceilings.json`/`resolve_ceiling`); short-tag+one-line reason detail. Adopted defaults: `notify_send` on every trip, **FAIL-OPEN** everywhere (malformed guards file ⇒ env defaults; the breaker is never silently disabled), `log_event circuit_tripped` carries `kind`(consecutive|retry_budget)/`fails`/`attempts`, **NO dashboard change** (`[?]` + note + push are the surfaces). New driver helpers `attempts_get`/`failreasons_get`/`failcount_reset_consecutive`/`resolve_guards`; reset is now completion-aware (full reset on `[x]`, consecutive-only reset on a clean-but-incomplete tick so attempts survive). `test/circuit-breaker.bats` grew 5→11 (retry-budget trip, flapping survival, completion full-reset, notify fires, per-item override, fail-open); format updates rippled into `test/verification.bats` + `test/per-item-ceiling.bats` (the failcount value is now a record); suite **212/212**. Driver-only — no D1 migration. Behaviour change (RETRY_BUDGET defaults to 6, format changes) lands only on the gated rollout (a daemon picks it up via `sync --restart-only`); origin lands the commit inert until then.
- [x] **(P1 · W2) Per-item token/turn ceiling.** SHIPPED 2026-05-29 (`docs/per-item-ceiling.md`). [pause-primitive consumer, scope=ITEM — the finest of the four pause scopes] A single hard item can spiral (many turns, huge spend) WITHIN one tick, below the per-project budget's *rolling*-window radar (and below the `CLAUDE_TIMEOUT_SECS` wall-clock watchdog, which bounds time, not turns/tokens). **Two layers:** (1) **PREVENTIVE** — when a per-item USD cap is set the driver passes claude `--max-budget-usd` so it self-stops before overspending; NB this CLI (v2.1.157) has **no `--max-turns`** (the bring-up design assumed it), so the only native preventive cap is the dollar one and turns/tokens are caught post-hoc. (2) **POST-HOC** (Guard 6 in `tick_once`) — reads THIS session's actual `num_turns`+headline-tokens from claude's **newest transcript** (the same `findNewestTranscript`/`extractUsage` source the status hook parses, headline = input+output+cache_creation = the ledger's `tokens` — so NO change to the `claude -p` text invocation, no `--output-format json`, no hook-ordering dependency) and, on a breach: an **INCOMPLETE** item (`[~]`) is **reopened** `[~]→[ ]` (owner stamp stripped, inline `<!-- ceiling: … -->` note appended) + **`failcount_bump`** (same counter as exit≠0/verify-fail → composes with the Guard-1 circuit breaker → `[?]` after K), keeping any commit (no auto-revert, mirrors Guards 2/3/4); a **COMPLETED** item (`[x]`) is **logged, not reopened** (reopening finished work would only re-spend — design call #5; Guard 6 is thus mutually exclusive with Guard 4, which only fires on `[x]`). **DECISIONS (user, 2026-05-29):** mechanism=both · shape=**inert/opt-in** (absent ⇒ uncapped; `MAX_TURNS_PER_TICK`/`MAX_TOKENS_PER_TICK`/`MAX_BUDGET_USD_PER_TICK` env defaults + a `.claude/backlog-ceilings.json` `{max_turns,max_tokens,max_budget_usd,items:{}}` with per-item-title substring overrides; precedence env→project→per-item) · token source=newest transcript · failcount=same counter · FAIL-OPEN on unparseable usage · surfacing=`log_event item_ceiling` only (no dashboard) · **NOT infra-exempt** (a runaway *task* is a bug-catch, not a fleet policy pause — and a spiraling infra item is the most dangerous). Driver-only + CLI-test-only — **NO D1 migration / dashboard change**; ships inert/opt-in. `test/per-item-ceiling.bats` (11), default-disabled via `CEILING_DISABLE` in `test/helpers.bash`; suite **206/206**.
- [x] **(P1 · W2) Per-project budget *enforcement* (not just warnings).** SHIPPED 2026-05-29 (`docs/per-project-budget.md`). [pause-primitive consumer, scope=project] `backlog-budgets.json` only ever *colored the dashboard*; now it ENFORCES. When a project's own rolling token spend exceeds a per-project cap, its daemon pauses to **heartbeat-only** (alert ONCE) and **resumes on its own** as the rolling window drains under the cap — self-imposed *spend* control, distinct from the account-level *plan-limit* cooldown (Anthropic's hard cap). The signal is **LOCAL + per-project** (no D1): a plain sum of the project's own git-tracked `.claude/backlog-history.jsonl` `tokens` over the window (so the sum spans every host = project-total spend). **DECISIONS (user, 2026-05-29):** (1) **both** rolling-5h AND rolling-week windows enforce (pause if either over cap); (2) cap scope = **project total across all hosts**; (3) **honor the Layer-3 fallback** (a budget cap is on *Anthropic* spend, so a non-Claude fallback may still drive the tick — mirrors the cooldown block, not freeze/pause); (4) schema = optional `projects: { "<name>": { rolling_5h_tokens, rolling_week_tokens } }` map, **absent ⇒ uncapped** (opt-in). Adopted: **fail-OPEN** on any missing/unreadable input, infra **EXEMPT** (`freeze_exempt`), claim retained, alert-once-then-throttle (reset on resume), `backlog-agents` banner (`budget: OVER CAP …`) + `doctor` validation of the map + `log_event budget_paused`/`budget_alert`. Driver (`_budget_over_cap`/`_budget_alert_once` + top-of-tick short-circuit after cooldown) + CLI only — **NO D1 migration / dashboard change**; ships **inert/opt-in** (no project capped until one is added to `~/.claude/backlog-budgets.json`). Per-host state `.claude/backlog-agent-budget.json` gitignored (infra + template). `test/per-project-budget.bats` (10: over-5h/over-week/cross-host/under-cap/infra-exempt/fail-open-no-file/fail-open-no-entry/disable-flag/fallback-honored/alert-once-throttle-reset); suite **195/195**.
- [x] **(P1 · W2) Secret-scanning pre-push gate.** SHIPPED 2026-05-29 (`docs/secret-scan.md`). [guard-primitive consumer] A `gitleaks`/`trufflehog`-style gate so a daemon (or a human) can **never push a key/token** to a repo the fleet manages — important because agents commit unattended to repos that may be public. **THE CRUX:** every other W2 guard is POST-push (flag/reopen an already-pushed commit), but a published secret can't be un-published, so prevention must run BEFORE the push. Since the daemon doesn't mediate claude's `git push`, prevention lives in a per-clone git **`pre-push` HOOK** (`.git/hooks/pre-push`) that runs the scanner and **rejects the push (exit 1)** on a hit — catching claude's own push, the daemon's claim/status pushes, and a human's push uniformly. A belt-and-suspenders **Guard 5** in `tick_once` still DETECTS + alerts (`log_event secret_detected` + `notify_send`, flag-only like Guards 2/3) if a clone is missing the hook (fresh machine pre-first-tick, hand-edited `.git/hooks`, `push --no-verify`) — it can't un-publish but it surfaces the leak so the key gets rotated. **DECISIONS (user, 2026-05-29, all recommended):** (1) **pre-push hook + post-push flag** (hook = true prevention, Guard 5 = belt-and-suspenders); (2) **gitleaks-if-present + always-on builtin regex floor** (AWS/PEM/`ghp_`/`sk-`/`sk-ant-`/`xox`/`AIza`/Stripe/webhooks/GitLab/SendGrid — high-confidence, low-FP; neither machine has gitleaks so the floor is mandatory — the gate is never silently absent); (3) **ensure-on-tick + doctor + `install-hooks`** (driver self-heals the hook every tick keyed by `PREPUSH_HOOK_VERSION`; `doctor` asserts it; `backlog-agents install-hooks [--force]` is the one-shot fleet-wide install); (4) **fail-CLOSED on a hit, fail-OPEN on absent/scanner-error** (a broken/missing scanner must not wedge the fleet — but the builtin floor means "no gitleaks" ≠ "no gate"), with a committed allowlist (`.gitleaks.toml`, or builtin: inline `pragma: allowlist secret` / a `.secret-scan-allow` regex file). New standalone `bin/backlog-secret-scan` (`--prepush`/`--range`/`--commit`/`--staged`; ONE home for the regex set, sourced by the hook + Guard 5 + CLI; jq/_lib-free so the tiny hook can exec it). Shared `prepush_hook_body`/`PREPUSH_HOOK_VERSION`/`PREPUSH_HOOK_MARKER` + foreign/current detectors in `_lib.sh`. A **NON-EMPTY** unmarked hook is treated as a FOREIGN user hook (never clobbered without `--force`); an empty (0-byte) hook — the state all 7 clones were found in — is overwritten. **Driver + CLI + per-clone hooks only — NO D1 migration / dashboard change.** `test/secret-scan.bats` (20: scanner modes, real git push rejection, builtin/gitleaks dispatch, allowlist, fail-open, ensure-on-tick install/overwrite-empty/leave-foreign/rewrite-stale, Guard 5 flag+alert, install-hooks); suite **185/185**; gated behind `SECRET_SCAN_DISABLE` (default-disabled in `test/helpers.bash`, like FREEZE/ACCOUNT_COOLDOWN). Propagates fleet-wide via the gated driver rollout (canary+skew); the hook rides ensure-on-tick + a one-time `install-hooks` per machine. Inert/fail-open until rolled.
- [x] **(P1 · W2) Dead-man's-switch alerting — proactive, not passive.** SHIPPED 2026-05-29 (`docs/dead-mans-switch.md`). [notify-channel consumer] The motivating incident: the cooldown-add bug *knew* it was failing (`fatal: … did not match any files` every tick) and the dashboard *showed* STALE, but nothing pushed an alert to the user for ~8h. Promotes "STALE-too-long / repeated-hook-failure" from a passive dashboard color into an ACTUAL push (Slack / email / macOS). **Two detectors, two homes** (a dead daemon can't alert about itself): **(b) repeated-hook-failure** lives in the DRIVER (`bin/backlog-agent`) — `_run_status_hook` feeds `_hookfail_record`, which counts consecutive `status_fail` in a per-host `.claude/backlog-agent-hookfail.json` and fires ONE `notify_send` at `DMS_HOOKFAIL_THRESHOLD` (=3, matches the circuit breaker) then throttles via an `alerted` flag, resetting on `status_ok`; **(a) STALE-too-long** lives in the CLI monitor (`backlog-agents monitor` Pattern 8) — reads the INDEPENDENT D1 heartbeat per (project,host) (enumerated from the synced per-host status files) and fires when one is older than `DMS_STALE_SECS` (=3600 = 2× `HEARTBEAT_SECONDS`), with per-incident dedup, FAIL-OPEN on an unreadable heartbeat, and infra EXCLUDED (held by policy). **DECISIONS (user, 2026-05-29):** ship BOTH halves + install the monitor on M1; ONE canonical notify config `~/.claude/agent-notify.json` (`slack_webhook_url`/`email`/`local_notify`, jq-free read; monitor falls back to the legacy `~/.config/backlog/notify.json`); detector (a) in the monitor now with the independent **Cloudflare cron worker** (the only catcher of *whole-machine* death) as a noted follow-up; N=3. `notify_cooldown` refactored to share `notify_send` (one config + code path). **Driver-only + CLI-only — NO D1 migration / dashboard change**; propagates fleet-wide via the gated driver rollout, inert until the notify config is set + the monitor is installed. `test/dead-mans-switch.bats` (9); suite **164/164**.
- [x] **(P1 · W2) Per-tick outcome verification.** SHIPPED 2026-05-29 (`docs/per-tick-verification.md`). The first **vetoing** tick-guard (Guard 4; Guards 1/2/3 only count/warn). `tick_once` treated `claude -p` exit 0 + claude's self-report as success — but exit 0 is not proof the gate is green: claude can flip an item to `[x]` and commit while typecheck/tests are broken, and the daemon records silent "progress." **Fix:** after an exit-0 tick that CLAIMED COMPLETION (item now `[x]` + a work commit landed), the daemon independently runs the project's gate on the committed HEAD; on failure it REOPENS the item (`[x]`→`[ ]`, owner stamp stripped) + `failcount_bump` so K consecutive verification failures trip the existing Guard-1 circuit breaker → `[?]`, rather than recording success. **DECISIONS (user, 2026-05-29):** (1) **gate source** = `.claude/backlog-gate.json` `{"gate":[...]}` (array=argv, string=`bash -c`), else a `package.json` `verify` / `typecheck`+`test` fallback (npm's stock "no test specified" placeholder ignored), else **FAIL-OPEN** (no gate ⇒ skip = prior behaviour); (2) **trigger** = only when the item was flipped to `[x]` this tick (`_completion_claimed`; partial-progress + `[?]` flips don't fire); (3) **on failure** = reopen + bump (composes with the breaker), **keep the commit** (no auto-revert — already-pushed-commit revert is a cross-machine hazard, mirrors Guards 2/3); (4) **timeout** = `GATE_TIMEOUT_SECS=600` under a pure-bash watchdog, overrun = failed verification. Runs AFTER `_maybe_auto_complete`, BEFORE `TICK_OUTCOME=work`; `work_base..work_head` snapshotted once so Guards 2/3 + the fleet-freeze auto-arm never see the reopen commit; **fleet-freeze auto-arm gated on a PASSING gate** (verify first — never freeze the fleet on a `bin/` commit that failed its own gate). Telemetry: `verify_passed` / `verify_failed` events (no new D1 surface). `test/verification.bats` (10); suite **155/155**; driver-only (no D1 migration / dashboard change). Inert until a project defines a gate, so it propagates safely fleet-wide (canary+skew gated) and each project opts in later.
- [x] **(P1 · W2) Account-level shared cooldown.** SHIPPED 2026-05-29 (`docs/account-cooldown.md`). Pause-primitive consumer at scope=fleet, rides the D1 bus. The Anthropic plan limit is **per-account** (shared across every machine + project), but `start_cooldown` armed cooldown *per-project, per-machine* — so when one daemon hit the cap the other 11 daemon×project combos each had to fail their own `claude -p` call before arming a local cooldown. Now a single account-level signal on D1 makes the **whole fleet stand down at once** the moment ANY daemon detects the limit, and resume together at the reset. **Carrier (DECISION):** a **dedicated** `account_cooldown` table + `GET/POST /api/cooldown` endpoint (backlog-dashboard `migrations/0004` + `functions/api/cooldown.js`), NOT folded into `fleet_control` — the cooldown carries an `until_epoch`, auto-expires, and falls through to the Layer-3 fallback agent (distinct from the deliberate freeze/pause/stop holds). **WRITE:** `start_cooldown` (`bin/backlog-agent`) also publishes the reset via `_d1_publish_cooldown` (jq-free, bearer, backgrounded best-effort like `notify_cooldown`); endpoint keeps the **LATER** `until_epoch` (MAX) across concurrent writers (DECISION: a shorter fallback window can't truncate a parsed one). **READ:** `_d1_account_cooldown_epoch` (jq-free, mirrors `_d1_heartbeat_epoch`, **FAIL-OPEN**) is consulted at top-of-tick Layer 2, OR'd with the local `cooldown_active` (account = fleet-wide superset; D1 down ⇒ local still governs — never a regression); honors the Layer-3 fallback fall-through; auto-expires at `until_epoch` (no manual clear). **CLI (`bin/backlog-agents`):** banner `cooldown: ACCOUNT-WIDE …` line (active only) + `cooldown --status`/`--check`/`--clear` (manual lapse for a bad parse). Tests: `test/account-cooldown.bats` (13: driver gate/publish + CLI); suite **145/145**. **Rollout (outward-facing, gated):** apply `migrations/0004` + deploy Pages (bootout the live dashboard daemon first), then push the driver (`daemon-sync` propagates gated on canary+skew; M3 pulls + canary). Inert until both endpoint + new driver are live (404 ⇒ fail-open to existing per-project behaviour).
- [x] **(P2 · W3) Fleet pause / stop / resume control (all machines).** SHIPPED 2026-05-29 (fleet scope). The MANUAL sibling of fleet-freeze, on the SAME D1 `fleet_control` table (resolves fleet-freeze DECISION F: one table, precedence **stop > pause > freeze**). `backlog-agents pause | resume | stop | start` (+ `--reason`, `--status`, `--check`); the driver does ONE combined read at top-of-tick (`_d1_control_get`, jq-free, **fail-open** for all three modes) and switches on the endpoint-resolved `mode`: **pause** ⇒ heartbeat-only hold (infra-EXEMPT like freeze; claim retained), **stop** ⇒ full halt (no exemption — the daemon exits its loop; launchd respawns + re-exits so it stays effectively down, no heartbeat, until `start`; `launchctl bootout` is the hard manual backstop; the `[~]` claim is reaped once the D1 heartbeat lapses). Implemented as additive rows (`key='pause'|'stop'`, reusing the `frozen` active-bit column — no schema change to 0002): backlog-dashboard `migrations/0003_fleet_control_modes.sql` + a back-compat extension of `GET/POST /api/fleet-control` (GET resolves precedence → `mode` + per-control `controls` map, still returns top-level `frozen` so the pre-pause/stop driver keeps honoring freeze through the rollout; POST takes a `key`, default `freeze`). `backlog-agents` banner surfaces the active control (`control: STOPPED/PAUSED/FROZEN …`). Tests: `test/fleet-pause.bats` (14: driver gate stop/pause/exempt/fail-open/back-compat + CLI arm/clear/check per row); suite **132/132**. DECISIONS: pause+stop fail-OPEN, pause infra-exempt, stop relies on the TTL reaper. **DEFERRED** (in `fleet-control-design.md`): project/item scope, auto-expiring pause, `--except` targeting, the stop sentinel + cross-machine restart watchdog.
- [x] **(P2 · W3) `do_sync` must EXCLUDE the infra daemon from auto-restart (fleet-freeze sibling gap).** SHIPPED 2026-05-29. Root cause of the "held daemon was actually running" incident: `do_sync`'s restart loop bootstrapped **every** `com.$USER.*.backlog-agent.plist` incl. `backlog-infra`, so any `sync`/`daemon-sync` cycle re-bootstrapped + un-held the infra daemon (which self-modifies the shared `bin/` and `reset --hard`s the tree — it wiped in-flight interactive edits). **Fix:** `bin/backlog-agents` gained `INFRA_PROJECT` (default `backlog-infra`) + `_is_infra_project`; `do_sync`'s restart loop now **skips the infra daemon** (loud `warn`, `continue`) unless `--include-infra` is passed — the restart-path twin of the fleet-freeze `freeze_exempt` tick-loop carve-out. Help/usage updated. `test/sync-infra-exclude.bats` (3, launchctl-shimmed: default-skip · `--include-infra` opt-in · follows `INFRA_PROJECT`); suite 118/118; canary re-validated. The infra daemon + `daemon-sync` can now be re-enabled without un-holding infra (infra stays held until `--include-infra` is explicitly used).
- [x] **(P2 · W3) Fleet freeze on in-flight driver changes (infra = highest priority).** SHIPPED 2026-05-29 (`docs/fleet-freeze.md` now SHIPPED). When a *fleet-affecting* (`bin/`) infra change is in flight, a single fleet-wide D1 flag (`fleet_control` row, backlog-dashboard `migrations/0002` + `GET/POST /api/fleet-control`) holds every **non-exempt** daemon to heartbeat-only at top-of-tick (`bin/backlog-agent` `_d1_freeze_get`, jq-free + **fail-open** so a control-plane outage never stalls the fleet; `TICK_OUTCOME=freeze`). `backlog-infra`'s OWN daemon is **EXEMPT** (`freeze_exempt`) — it keeps ticking, runs `backlog-agents freeze --eval`, and lands+verifies the fix, avoiding the deadlock where the fixer freezes itself. **Auto-arm** on a `bin/` work-commit (the exempt daemon only); **auto-clear** only when `_sync_gate_check` (canary-green + local skew-zero) AND `_fleet_skew_zero` (every live host on the ref SHA, 90-min liveness window) both hold; `backlog-agents freeze`/`unfreeze` are the manual arm/override. Tests: `test/fleet-freeze.bats` (14); suite 112/112; canary re-validated the new driver. Decisions A/B/C/E/F resolved (auto-arm · 90-min window · fail-open gate · dedicated endpoint · single keyed table); D (clear-evaluator when infra daemon is down) left to manual `unfreeze` + a follow-up watchdog. **Surfaced a sibling gap → new Thinking item:** `do_sync` re-bootstraps the infra daemon, un-holding it. backlog-infra daemon + daemon-sync currently held on M1 pending that fix.
- [x] **(P2 · W1) TaizMail-class orphan-stash leak: tracked status/history files pop-conflict under concurrent cross-machine writes.** Found 2026-05-29: TaizMail accumulated 10 orphan stashes whose content was `M .claude/backlog-history.jsonl` + per-host status — the status hook's pre-rebase autostash captured those tracked telemetry files, and when origin had also changed them the post-rebase `git stash pop` conflicted; the failed pop was only *logged* (lines 512-519), never dropped, so it orphaned and silently piled up. **Fix (2026-05-29):** `bin/backlog-agent-status.mjs` gained `popAutostash()`, which replaces the inline pop. On a pop conflict it lists the unmerged paths; if they are **confined to the hook-owned ephemeral set** (`.claude/backlog-status*.json`, `backlog-history.jsonl` — files this same run rewrites/re-appends regardless), it auto-resolves (line-**union** for the append-only history via `git show HEAD:` ⊎ `git show stash@{0}:`, take-origin for the rewritten status JSONs), `git add`s them, and **drops the stash** so it can never orphan. A conflict touching **any other path** is real agent work → unchanged legacy warn-and-leave (stash preserved). No history lines are lost (the union keeps base/origin/local + this tick's line). Two regression tests added to `test/status-hook.bats` (ephemeral-conflict→resolved+dropped; non-ephemeral→preserved); full suite green (101 tests). This is the interim mitigation; the **D1 telemetry bus (W1, COMMITTED)** still removes git-borne telemetry entirely as the root-cause fix. **Residual cleanup (not code):** 10 junk stashes still sit on TaizMail's M1 clone (user opted backlog-only) — inspect-per-stash + drop; and the sacred-geography `refs/orphan-rescue/backlog-2026-05-29T0628Z` rescue ref (tag `orphan-rescue-backlog-0628`, audited to hold only compacted prose, no open/blocked items) can be dropped once confirmed unneeded.
- [x] **(P1 · W2) Remote unblock — answer a `[?]` by replying to the fleet digest.** SHIPPED 2026-05-30 (`docs/remote-unblock.md`). The FIRST inbound control path (everything prior was outbound notify or daemon-polled-D1-flag). A `[?]` blocker could only be answered on the clone-owning machine (edit `docs/Backlog.md` + `backlog-agent unblock`); now the user replies to the Telegram digest and the answer lands in the right project's backlog from outside any clone. Rides the proven D1 poll-and-apply bus. **DECISIONS (user, 2026-05-30):** ship the FULL loop incl. a Telegram bot; D1 row daemon-applied write path; addressing = `<project>#<index>` + title-prefix guard (stale index ⇒ no-op + re-notify, never the wrong item); shared `HEALTH_API_KEY` bearer on the D1 write (GET public). **Dashboard (backlog-dashboard):** `migrations/0005_unblock_requests.sql` + `functions/api/unblock.js` (GET pending per project, public/read-only, robust-to-unmigrated; POST publish/clear bearer-gated, idempotent CAS clear) + `functions/api/telegram-webhook.js` (receives Telegram updates; two auth gates — `X-Telegram-Bot-Api-Secret-Token` == `TELEGRAM_WEBHOOK_SECRET` AND `chat.id` == `TELEGRAM_CHAT_ID`; parses `<project>#<N> <answer>`, writes the row via the D1 binding, acks via sendMessage). **Driver (`bin/backlog-agent`):** `UNBLOCK_URL`/`REMOTE_UNBLOCK_DISABLE`, `_d1_unblock_get` (jq-free curl, node-parsed into `id|item_index|title_prefix_b64|answer_b64` — `|`-delimited + both fields base64 so an empty prefix survives `read`; FAIL-OPEN), `_d1_unblock_clear` (backgrounded bearer POST), `_unblock_apply` (range + title-prefix guard → write `[user]` block + flip `[?]`→`[!]` via awk with the answer passed through the env → atomic commit + push → clear row; `log_event unblock_applied`/`unblock_mismatch`), `process_unblock_requests` hooked into `tick_once` right before `auto_flip_blocked`. **CLI (`bin/backlog-agents`):** `do_digest` renders the stable `<project>#<N>` handle + a reply hint in the stdout list and the Telegram body. **Prerequisite (user, outside code):** create a `@BotFather` bot, add `telegram_bot_token`/`telegram_chat_id` to `~/.claude/agent-notify.json`, set the CF secrets, and register the webhook (`setWebhook?...&secret_token=…`). `test/remote-unblock.bats` (5, hermetic curl-shim); suite **224/224**.

- [x] **Systemic: output-channel flakiness → architectural hallucination cascade.** 2026-05-30. Session `4e3c5c25` burned ~272M tokens. Root cause: terminal-output lag caused empty `Read` results → Claude concluded a 13.8 KB spec file was blank → hallucinated the driver's structure (invented `--once`/`--max-iterations`/`plan_item`/`apply_item`/`run_iteration`/`retry()` — none exist; the real driver is `tick_once`, ~3528 lines) → built a bogus `test/12-inject-fault.bats` + a false memory note claiming tests passed → asked the user design questions based on fabricated architecture. Claude self-detected and corrected. Mitigations shipped this session: **(a)** global `~/.claude/CLAUDE.md` `## Resilience — tool-output integrity` section (5 rules: empty-read retry, edit-failure signal, contradiction re-ground, verify-before-questions, verify-before-memory); **(b)** per-project anti-hallucination anchors in `backlog-infra/CLAUDE.md` (4 rules: real driver structure, read locking doc, verify edits landed, no memory without observed output); **(c)** resilience rules added to all 5 active project CLAUDE.mds (tool-output-lag amplified for dashboard, AppleScript-edit-verification for TaizMail, What NOT to do for sacred-geography); **(d)** template `CLAUDE.md` so `dev-projects new` scaffolds new projects with the resilience section built-in; **(e)** per-project token budget caps activated for all 6 active projects in `~/.claude/backlog-budgets.json` to catch future token-burn outliers (budget enforcement shipped separately in a prior session, this session just activated the opt-in caps).
