node_modules/
dist/
coverage/
.DS_Store

# Per-user Claude Code config
.claude/settings.local.json
.claude/scheduled_tasks.lock
.claude/backlog-agent.log
.claude/backlog-agent-events.jsonl
.claude/backlog-agent-failcounts.json
.claude/agent-cooldown.json
.claude/backlog-agent.lock/
.claude/backlog-agent.tick.lock/
.claude/watch-backlog.ping
.claude/launchd-stdout.log
.claude/launchd-stderr.log
# Shared headline status — written every tick but never committed (the hook
# commits only the per-host + history files), so tracking it just churns the
# working tree and piles up auto-stashes. The fleet dashboard reads it locally.
.claude/backlog-status.json

# OpenCode workspace config
opencode.json
